def PrimeGenerator(NumPrimesToGen,PrimeSeed=[2],NumLoopIters=None):
  if NumPrimesToGen >= 1 and NumLoopIters==None:
    #1st Config: Generate to a number of prime numbers
    pass
  if NumLoopIters != None and type(NumLoopIters)==int and NumPrimesToGen==None:
    #2nd Config: Generate prime numbers to a set number of loop iterations.
    pass
